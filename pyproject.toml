[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nanogpt-phase2"
version = "0.1.0"
description = "nanoGPT Phase 2: Instruction SFT, LoRA Parameter-Efficient Fine-Tuning, and DPO Alignment."
readme = "README.md"
authors = [{ name = "AI Engineering Team" }]
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "tiktoken",
    "onnx"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["*"]
exclude = ["data*", "config*", "assets*", "build*", "dist*"]
